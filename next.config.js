/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // We can add more configurations here later, like image optimization settings or redirects.
};

export default nextConfig;
